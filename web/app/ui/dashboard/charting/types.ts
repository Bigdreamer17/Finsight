import type { chartType } from "../common/types";

export type chartDataType = {
  revenue: chartType[];
  net_profit: chartType[];
  eps: chartType[];
  assets: chartType[];
  equity: chartType[];
};
